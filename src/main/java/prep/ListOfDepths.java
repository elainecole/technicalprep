package prep;
/**
 * 4.3 List of Depths: Given a binary tree, design an algorithm which creates 
 * a linked list of all nodes at each depth (e.g., if you have a tree with 
 * depth D, you'll have D linked lists). 
 * 
 * 5/12/20 
 * 
 */
public class ListOfDepths {
    public static void main(String[] args) {
    }
    /*
    // RNode  newr = new RNode(n);
    // newr.next = l.list;
    // l.list = newr;

    // pre-order traversal 
    void preOrderTraversal(Node root, Node bList, int depth) {
	// first act on node root, then traverse children 
	if (root == null) return; // end case
	depth++;
	storeNodeGivenDepth(root, depth, bList); 
	preOrderTraversal(root.left, bList, depth); 
    preOrderTraversal(root.right, bList, depth); 
}
*/
}